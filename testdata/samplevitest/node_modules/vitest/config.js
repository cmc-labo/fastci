// A minimal fake standing in for the real "vitest/config" module's
// defineConfig export - see package.json in this directory.
exports.defineConfig = function defineConfig(config) {
  return config;
};
